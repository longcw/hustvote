<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hustvote.hustvote" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="com.hustvote.hustvote.permission.SINA_PUSH" />

    <permission
        android:name="com.hustvote.hustvote.permission.SINA_PUSH"
        android:protectionLevel="normal" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".ui.LoginActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.HallActivity"
            android:label="@string/title_activity_hall" >
        </activity>

        <activity
            android:name=".ui.HallFragmentActivity"
            android:label="@string/app_name"
            android:logo="@drawable/logo"

            />

        <activity android:name=".ui.ChoiceIntroFragmentActivity" android:label="@string/choice_intro" />

        <activity
            android:name=".ui.VoteActivity"
            android:label="VoteActivity_WebView" />
        <activity
            android:name=".ui.ChoiceIntroActivity"
            android:label="ChoiceIntro" />
        <activity
            android:name=".ui.RegisterActivity"
            android:label="@string/title_activity_register" >
        </activity>
        <activity
            android:name=".ui.ResultActivity"
            android:label="Result" />
        <activity android:name=".ui.QRCodeScannerActivtiy" />
        <activity android:name=".ui.NoticeActivity" />


        <activity
            android:name=".ui.ReviewActivity"
            android:label="@string/title_activity_review" >
        </activity>


        <!-- ************* 以下内容是SAE云推送功能 ************* -->


        <!--
        	声明使用SinaPushSDK必须启动的服务com.sina.push.service.SinaPushService
        	注意：需要有 android:permission="com.sina.permission.SINA_PUSH"
        -->
        <service
            android:name="com.sina.push.service.SinaPushService"
            android:label="PushDataCenter"
            android:permission="com.hustvote.hustvote.permission.SINA_PUSH"
            android:process=":remote" >
            <intent-filter>

                <!--
                	SinaPushSDK service action命名规则  sina.push.action.service.APPID
                	注意必须为： sina.push.action.service. + APPID
                -->
                <action android:name="sina.push.action.service.21592" />

                <category android:name="android.category.DEFAULT" />
            </intent-filter>
        </service>

        <!-- 声明使用SinaPushSDK 接收透传消息的广播事件 -->
        <receiver android:name=".net.push.SAEMsgReceiver" >
            <intent-filter>
                <action android:name="com.sina.push.msg.broadcast.21592" />
            </intent-filter>
        </receiver>

        <provider
            android:name="com.sina.push.datacenter.PushProvider"
            android:authorities="com.sina.push.pushprovider.21592"
            android:multiprocess="false"
            android:process=":remote" />

        <meta-data
            android:name="sinapush_appid"
            android:value="21592" />


    </application>

</manifest>
